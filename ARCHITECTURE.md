# AI PPT Generator - 项目架构文档

> 一个基于 AI 的智能 PPT 创作平台，让演示文稿制作变得简单高效。

---

## 📁 项目结构

```
AI-PPT-Generator/
├── Backend/                    # 后端服务
│   ├── app/                   # 核心应用代码
│   │   ├── core/             # 核心配置
│   │   ├── models/           # 数据模型
│   │   ├── routers/          # API 路由
│   │   ├── schemas/          # Pydantic 模型
│   │   ├── services/         # 业务逻辑
│   │   ├── tasks/            # Celery 异步任务
│   │   ├── utils/            # 工具函数
│   │   ├── config.py         # 配置文件
│   │   ├── database.py       # 数据库连接
│   │   └── main.py           # 应用入口
│   ├── alembic/              # 数据库迁移
│   ├── docker/               # Docker 配置
│   ├── scripts/              # 脚本工具
│   ├── tests/                # 测试文件
│   ├── requirements.txt      # Python 依赖
│   └── Dockerfile            # 容器配置
│
├── frontend/                  # 前端应用
│   ├── app/                  # Next.js App Router
│   │   ├── layout.tsx        # 根布局
│   │   ├── page.tsx          # 首页
│   │   └── globals.css       # 全局样式
│   ├── components/           # React 组件
│   │   ├── Navbar.tsx        # 导航栏
│   │   ├── FloatingShapes.tsx # 背景动画
│   │   └── FeatureCard.tsx   # 功能卡片
│   ├── public/               # 静态资源
│   ├── package.json          # Node 依赖
│   ├── tailwind.config.ts    # Tailwind 配置
│   └── next.config.js        # Next.js 配置
│
└── CHANGES_REPORT.md         # 变更记录
```

---

## 🖥️ 后端架构 (Backend)

### 技术栈

| 组件 | 技术 | 用途 |
|------|------|------|
| **Web 框架** | FastAPI | 高性能异步 API |
| **数据库** | PostgreSQL | 主数据库 |
| **ORM** | SQLAlchemy 2.0 | 数据库操作 |
| **缓存** | Redis | 会话、缓存 |
| **任务队列** | Celery | 异步任务处理 |
| **迁移工具** | Alembic | 数据库版本管理 |
| **认证** | JWT + OAuth2 | 用户身份验证 |
| **加密** | AES-256 | 敏感数据加密 |

### 核心功能模块

```
┌─────────────────────────────────────────────────────────┐
│                      API Gateway                        │
│                      (FastAPI)                          │
└─────────────────────────────────────────────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
┌──────────────┐   ┌──────────────┐   ┌──────────────┐
│   用户模块    │   │   PPT模块    │   │   AI模块     │
├──────────────┤   ├──────────────┤   ├──────────────┤
│ • 注册/登录   │   │ • CRUD操作   │   │ • 内容生成   │
│ • JWT认证    │   │ • 单页编辑   │   │ • 模板选择   │
│ • API Key    │   │ • 撤销/重做  │   │ • 异步处理   │
└──────────────┘   └──────────────┘   └──────────────┘
        │                  │                  │
        └──────────────────┼──────────────────┘
                           ▼
              ┌──────────────────────┐
              │    数据持久化层       │
              ├──────────────────────┤
              │ PostgreSQL (主库)    │
              │ Redis (缓存/会话)    │
              │ Celery (任务队列)    │
              └──────────────────────┘
```

### API 设计

| 模块 | 端点 | 功能 |
|------|------|------|
| **认证** | `POST /api/v1/auth/login` | 用户登录 |
| | `POST /api/v1/auth/register` | 用户注册 |
| | `POST /api/v1/auth/refresh` | 刷新 Token |
| **用户** | `GET /api/v1/users/me` | 获取当前用户 |
| | `PUT /api/v1/users/me` | 更新用户信息 |
| **PPT** | `GET /api/v1/ppts` | 列表查询 |
| | `POST /api/v1/ppts` | 创建 PPT |
| | `GET /api/v1/ppts/{id}` | 获取详情 |
| | `PUT /api/v1/ppts/{id}` | 更新 PPT |
| | `DELETE /api/v1/ppts/{id}` | 删除 PPT |
| | `POST /api/v1/ppts/{id}/slides` | 单页编辑 |
| **AI** | `POST /api/v1/ai/generate` | AI 生成内容 |
| | `GET /api/v1/ai/templates` | 获取模板 |
| **导出** | `POST /api/v1/export/pptx` | 导出 PPTX |
| | `POST /api/v1/export/pdf` | 导出 PDF |
| | `POST /api/v1/export/images` | 导出图片 |

---

## 🎨 前端架构 (Frontend)

### 技术栈

| 组件 | 技术 | 用途 |
|------|------|------|
| **框架** | Next.js 14 | React 全栈框架 |
| **语言** | TypeScript | 类型安全 |
| **样式** | Tailwind CSS | 原子化 CSS |
| **动画** | Framer Motion | 流畅动画 |
| **图标** | Lucide React | 矢量图标 |
| **字体** | Space Grotesk | 现代字体 |

### 设计系统

#### 色彩方案
```css
/* 主色调 - 紫粉渐变 */
--primary-purple: #667eea;
--primary-violet: #764ba2;
--primary-pink: #ec4899;
--primary-rose: #f43f5e;

/* 背景渐变 */
background: linear-gradient(135deg, 
  #667eea 0%, 
  #764ba2 25%, 
  #8b5cf6 50%, 
  #ec4899 75%, 
  #f43f5e 100%
);
```

#### UI 风格
- **玻璃拟态** (Glassmorphism)：半透明毛玻璃效果
- **动态渐变**：15秒循环的背景渐变动画
- **浮动元素**：多层级光斑营造深度感
- **微交互**：悬停、点击的精致动画反馈

### 组件架构

```
┌─────────────────────────────────────────────┐
│                  Layout                     │
│              (Next.js App)                  │
└─────────────────────────────────────────────┘
                      │
        ┌─────────────┼─────────────┐
        ▼             ▼             ▼
┌──────────┐  ┌──────────┐  ┌──────────┐
│  Navbar  │  │   Hero   │  │ Features │
│ (导航栏)  │  │ (主视觉)  │  │ (功能卡)  │
└──────────┘  └──────────┘  └──────────┘
        │             │             │
        └─────────────┼─────────────┘
                      ▼
        ┌─────────────────────────────┐
        │    FloatingShapes           │
        │    (背景动画层)              │
        └─────────────────────────────┘
```

### 页面结构

| 页面 | 路径 | 功能 |
|------|------|------|
| **首页** | `/` | 产品介绍、CTA |
| **登录** | `/login` | 用户认证 |
| **注册** | `/register` | 新用户注册 |
| **Dashboard** | `/dashboard` | PPT 管理 |
| **编辑器** | `/editor/[id]` | PPT 编辑 |
| **模板** | `/templates` | 模板选择 |

---

## 🔗 前后端交互

### 认证流程
```
┌──────────┐                    ┌──────────┐
│  Frontend │ ── POST /login ──▶ │  Backend │
│           │ ◀── JWT Token ──── │          │
│           │                    │          │
│ (存储Token)│                   │          │
└──────────┘                    └──────────┘
      │                                │
      │ 后续请求携带 Authorization Header │
      └───────────────────────────────▶│
```

### 数据流
```
Frontend          Backend
    │                 │
    │ ─── API Call ──▶│
    │                 │
    │ ◀── JSON Response
    │                 │
    ▼                 ▼
  React State    Database
```

---

## 🚀 部署架构

### 开发环境
```bash
# 后端
python -m venv venv
source venv/bin/activate
pip install -r Backend/requirements.txt
uvicorn app.main:app --reload

# 前端
cd frontend
npm install
npm run dev
```

### 生产环境
```
┌─────────────────────────────────────┐
│           Docker Compose            │
├─────────────────────────────────────┤
│  ┌──────────┐      ┌──────────┐    │
│  │ Frontend │      │ Backend  │    │
│  │ (Next.js)│      │ (FastAPI)│    │
│  │  :3000   │      │  :8000   │    │
│  └──────────┘      └──────────┘    │
│       │                 │          │
│       └─────────────────┘          │
│              │                     │
│  ┌───────────┴───────────┐         │
│  │      Nginx Proxy      │         │
│  │       (:80/:443)      │         │
│  └───────────────────────┘         │
└─────────────────────────────────────┘
              │
    ┌─────────┴─────────┐
    ▼                   ▼
┌──────────┐      ┌──────────┐
│PostgreSQL│      │  Redis   │
└──────────┘      └──────────┘
```

---

## 📊 项目统计

| 指标 | 后端 | 前端 |
|------|------|------|
| **代码行数** | ~15,000 | ~3,000 |
| **主要功能** | 20+ API | 6+ 页面 |
| **数据库表** | 8+ | - |
| **测试覆盖率** | 60%+ | - |

---

## 🛠️ 开发指南

### 后端开发
```bash
cd Backend

# 安装依赖
pip install -r requirements.txt

# 数据库迁移
alembic upgrade head

# 启动服务
uvicorn app.main:app --reload

# 运行测试
pytest
```

### 前端开发
```bash
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 代码检查
npm run lint
```

---

## 📚 相关文档

- [Backend/README.md](Backend/README.md) - 后端详细文档
- [Backend/DEPLOY.md](Backend/DEPLOY.md) - 部署指南
- [Backend/CODE_REVIEW.md](Backend/CODE_REVIEW.md) - 代码审查记录
- [CHANGES_REPORT.md](CHANGES_REPORT.md) - 变更记录

---

## 📝 版本历史

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-02-09 | v1.0 | 前端重构，Next.js + 活泼多彩风格 |
| 2026-02-04 | v0.9 | 后端完成，核心功能上线 |

---

<p align="center">
  Made with ❤️ by AI Slides Team
</p>
